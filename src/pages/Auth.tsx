import PageStub from "@/components/shared/PageStub";
const Auth = () => <PageStub eyebrow="Sign in" title="Enter the house." body="Authentication will be wired in once Lovable Cloud is enabled — say the word." />;
export default Auth;
